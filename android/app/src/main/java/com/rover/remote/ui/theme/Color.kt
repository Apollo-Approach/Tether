package com.rover.remote.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Background & Surface ───
val DarkBackground = Color(0xFF0F1117)         // Near-black with blue undertone
val DarkSurface = Color(0xFF161B22)            // GitHub-dark style surface
val DarkSurfaceVariant = Color(0xFF1C2333)     // Cards, panels
val DarkSurfaceElevated = Color(0xFF232A3B)    // Elevated cards, dialogs

// ─── Accent Colors ───
val Amber = Color(0xFFF59E0B)                  // Primary accent — warm gold
val AmberLight = Color(0xFFFBBF24)             // Lighter amber for highlights
val AmberDim = Color(0xFFD97706)               // Deeper amber for contrast
val AccentBlue = Color(0xFF60A5FA)             // Secondary accent — soft blue
val AccentPurple = Color(0xFF8B5CF6)           // Tertiary — violet

// ─── Semantic Container Colors ───
val UserBubble = Color(0xFF1E2A3A)             // User message background (cool dark blue)
val UserBubbleText = Color(0xFFE0F2FF)         // User message text
val AssistantBubble = Color(0xFF1F1D2B)        // Assistant message background (warm dark)
val AssistantBubbleText = Color(0xFFE5E7EB)    // Assistant message text
val SystemBubble = Color(0xFF2D1F1F)           // System/error message background
val SystemBubbleText = Color(0xFFFFB4AB)       // System message text

// ─── Text Hierarchy ───
val TextPrimary = Color(0xFFE5E7EB)            // Primary text
val TextSecondary = Color(0xFF9CA3AF)          // Secondary / muted text
val TextTertiary = Color(0xFF6B7280)           // Disabled / hint text

// ─── Status Colors ───
val StatusConnected = Color(0xFF34D399)        // Soft green
val StatusConnecting = Color(0xFFFBBF24)       // Warm amber
val StatusDisconnected = Color(0xFFEF4444)     // Warm red

// ─── Borders & Dividers ───
val DividerColor = Color(0xFF2D3748)           // Subtle divider
val OutlineColor = Color(0xFF374151)           // Input field borders

// ─── Aurora Gradient Colors ───
val AuroraAmber = Color(0xFFF59E0B)            // Warm gold
val AuroraOrange = Color(0xFFF97316)           // Warm orange
val AuroraMagenta = Color(0xFFEC4899)          // Warm pink
